# 筆電優化紀錄

日期：2026-06-03
機器：12th Gen Intel Core i7-1260P / 15.7 GB RAM / Windows 11 Pro

## 問題診斷

| 項目 | 狀態 |
|------|------|
| RAM 使用率 | 88%（13.8 / 15.7 GB），Memory Compression 啟動 |
| D: 可用空間 | 106 GB（優化前） |
| 啟動程式數 | 15 個，含多個重型 Electron App |
| User Temp | 706 MB |

## 已執行優化

### 1. 停用啟動項（7 個）
- electron.app.LobsterAI
- quark + QuarkUpdaterTaskUser1.0.0.21
- com.squirrel.Filo.Filo
- Teams
- MicrosoftEdgeAutoLaunch
- GoogleChromeAutoLaunch

**備份位置**：`HKCU\SOFTWARE\Microsoft\Windows\CurrentVersion\Run-Disabled-Backup`

**保留**：OneDrive、Warp、Google、Typeless、SecurityHealth、Logitech、SnagIt

### 2. 磁碟清理

| 項目 | 釋放空間 |
|------|---------|
| User Temp (`%TEMP%`) | ~394 MB |
| `D:\Claude\autohedge-env`（Python venv） | 1555 MB |
| **合計** | **~1.9 GB** |

**保留**：`D:\Claude\careerbot\web\node_modules`（仍在使用）

## 待辦

- [ ] 開機後確認 RAM 改善幅度
- [ ] 若 autohedge 重新需要：`pip install -r requirements.txt` 重建 venv
- [ ] Chrome 開啟 Memory Saver（設定 → 效能 → 記憶體節省模式）

## 還原方式

```powershell
# 還原任一啟動項（範例：Teams）
$src = 'HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\Run-Disabled-Backup'
$dst = 'HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\Run'
$val = Get-ItemProperty -Path $src -Name 'Teams'
Set-ItemProperty -Path $dst -Name 'Teams' -Value $val.Teams
```
